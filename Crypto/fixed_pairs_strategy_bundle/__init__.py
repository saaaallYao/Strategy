"""
Bundle of offline research assets for the fixed pairs reversion strategy.

This package exposes the backtest implementation so other modules
(e.g. fixed_pairs_pt) can reuse the exact same logic.
"""
